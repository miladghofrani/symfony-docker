nelmio_api_doc:
    documentation:
        info:
            title: Symfony + Docker
            description: A Docker-based installer and runtime for the Symfony 4.4 web framework.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
